---
interface Props {
  id?: string;
  className: string;
}
const { id, className } = Astro.props;
---

<section {id} class=`w-full flex justify-center ${className}`>
  <div class="w-[70%] md:w-4/5">
    <slot />
  </div>
</section>
